# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'everything Victoria' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'UltraDrawerView'
  # Pods for everything Victoria

  target 'everything VictoriaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'everything VictoriaUITests' do
    # Pods for testing
  end

end
